#version 330
uniform vec3 aa;
in vec3 oPosition_WorldSpace;
in vec3 oPosition_ObjectSpace;
in vec3 oPosition_EyeSpace;
in vec3 oNormal_InvWorldSpace;
in vec3 oNormal_ObjectScpace;
in vec2 oUVs;

in vec3 oCamera_WorldSpace;
in float oTime;


out vec4 colorOut;
 
void main()
{
    //colorOut = vec4(oUVs.x, 0.4, oUVs.y, 1.0);
	vec3 t = vec3(aa.x,0.3,aa.z);
	colorOut = vec4(aa, (0.5+0.4*sin(oTime)));
}